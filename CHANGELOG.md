# 1.9.0 — камера и жесты одной рукой

- Отдельная вкладка с выбором веб-камеры, зеркальным превью и скелетом обеих рук. MediaPipe Hand Landmarker работает локально в Web Worker, без записи кадров и облака.
- Жесты одной выбранной рукой: щипок для перетаскивания окна, V для разворачивания/восстановления, ладонь в сторону для переноса между мониторами. Вторая рука свободна.
- Режим видео: пауза/продолжить и перемотка ±5 секунд через выбранную медиасессию Windows с проверкой результата.
- Явное включение управления, проверка личности окна, сглаживание, выдержка жеста, пауза между действиями и остановка при потере руки/связи. Кулак, Esc и «Пятница, стоп» отключают управление.
- Исправлена загрузка устаревшего интерфейса из HTTP-кэша Electron после обновления. Настройки и авторизация не сбрасываются.
- 535 unit/API-тестов, реальный Worker/WASM на двух руках, проверка двух мониторов, воспроизведения и seek в собственной Edge-сессии; регрессии voice и Telegram UI.

# 1.8.1 — локальная формулировка сообщений Telegram

- Message Composer на Qwen 3.5 4B переводит косвенную речь в прямое сообщение до поиска чата и подготовки черновика. Личные сообщения не отправляются в Codex.
- Поддержаны «скажи», «передай», «спроси», перспектива отправителя/получателя, вопросы и дословный режим без модели.
- Строгая схема, проверки чисел, дат, ссылок, отрицаний и ролей; отказ с предложением дословного режима при недоступной модели или сомнительном результате. Стоп отменяет формулировку.
- Сохранены проверка получателя, подтверждение итогового текста, отмена черновика и проверка фактической отправки. [Подробности](docs/MESSAGES.md).
- Исправлен разбор заголовка `чат @ аккаунт` в Telegram; смена аккаунта после подготовки сообщения отменяет отправку.
- Новая иконка из GitHub используется в интерфейсе, favicon, окне и EXE. Повреждённые PNG/ICO пересобраны из исходного SVG; ICO содержит семь размеров от 16 до 256 px.

# 1.8.0 — Codex через подписку ChatGPT

- Провайдеры Codex CLI и Ollama, маршрутизация Hybrid/Local only/Codex vision. Для экрана по умолчанию Qwen 4B; в настройках можно выбрать Codex Luna low. Terra только по явному выбору. Авторизация ChatGPT переиспользуется без API key.
- Изолированный CLI с отключёнными исполнительными возможностями возвращает JSON; действия проверяет существующий Action Engine. Windows tools, UIA, Telegram, голос и подтверждения сохранены.
- Оптимизация снимков, увеличение фрагмента для мелкого текста и уточнение координат. Оригинал доступен для crop, в модель не отправляется без необходимости.
- Таймауты, локальный fallback, отмена Codex по «стоп», статусы и безопасные замеры. Поиск кнопки начинается с UIA и сам по себе её не нажимает.
- Самостоятельный тест и сравнение на одном снимке, реальные проверки двух дисплеев, Windows-приложений и Codex. Методика и ограничения: `docs/CODEX.md`.

# 1.6.3 — Устойчивость управления Windows

- После подтверждения vision-клика кнопка повторно находится на свежем снимке. Сравнение допускает небольшие анимации; корреляция области кнопки уточняет новые координаты. Большое изменение интерфейса отменяет действие.
- Единый разбор «экран/монитор/дисплей», словесных числительных и цифр. Нумерация из Windows DISPLAYn, аппаратный DeviceID и определение дисплея окна вместо собственных номеров по координатам.
- Локальные команды списка окон, активного окна, числа мониторов и положения окна работают без Qwen. Служебные окна скрыты, несколько окон программы сгруппированы.
- При ошибке, пустом кадре или таймауте PrintWindow — резервный захват видимой области окна. Перекрытое окно не подменяется содержимым другого приложения. Исходное свёрнутое состояние восстанавливается в finally, включая отмену.
- Без подтверждения доступны понятные безопасные действия UIA: навигация, вкладки, меню, play/pause, выбор и раскрытие списка. Опасные, неизвестные и координатные нажатия требуют подтверждения.
- В debug-логах только разрешённые технические поля; личный текст, значения полей, ответы модели и содержимое ошибок не записываются. System/health показывают выбранную модель.
- Планировщик принимает JSON в markdown-обёртке, сохраняя строгую проверку схемы инструментов. Расширены реальные Windows-проверки и изолированный запуск голосовых регрессий.

# 1.6.2 — Надёжность управления Windows и анализа экрана

- Снимок HWND без обязательного foreground: PrintWindow с отдельным процессом и таймаутом. Раздельный захват окна, монитора и всех мониторов.
- Прямые вопросы об экране сразу передают изображение в текущую Qwen 3.5 4B через Ollama; отключённый vision не делает снимков.
- Запуск и повторное открытие не считаются неудачей из-за отказа фокуса. Исключены скрытые служебные DWM-окна.
- Общий resolver с aliases, App Paths, стандартными путями, ярлыками и каталогом Windows / AppsFolder.
- UI Automation остаётся приоритетным. При отсутствии элемента — поиск рамки кнопки моделью, проверяемые координаты, подтверждение, клик и проверка результата.
- Локальный debug-журнал, отдельный `scripts/test_vision.py`, тесты Windows, vision и отказов. Голосовой тракт сохранён; добавлена текстовая остановка занятой цепочки.

# 1.6.1 — Распознавание речи

- Вместо Whisper Small установлена Whisper Large v3 Turbo, локальная GPU INT8/FP16; при недоступной CUDA используется та же модель на CPU.
- Одинаковая обработка русской речи для кнопки и голосовой активации: beam 5, словарь названий приложений, ограниченное усиление тихой записи и более мягкий VAD.
- Короткая пауза после обращения больше не отправляет обрезанную команду немедленно. Для ручной записи ожидание тишины увеличено до двух секунд, измерение уровня переведено на Float32.
- Записи с низкой уверенностью распознавания попадают в поле ввода для проверки, а не выполняются автоматически. Высокая оценка модели всё равно не гарантирует отсутствие ошибок.
- В настройках добавлены проверка распознавания, прослушивание собственной записи и подсказки о тихом/перегруженном микрофоне. Проверочная фраза не выполняется, запись хранится только в памяти вкладки.
- STT освобождает видеопамять после распознавания; перед следующим распознаванием освобождаются собственная модель Ollama и выразительный голос, чтобы модели помещались на RTX 4060 Ti 8 ГБ.
- На 12 синтетических русских записях новая конфигурация исправила подмену «Дискорд» на «диск» при тихой записи и ошибки в заметке с именем Алексея. Это ограниченная проверка, не измерение точности на голосе пользователя.

# 1.6 — Локальное управление компьютером

- Qwen 3.5 4B установлена в Ollama. В настройках можно выбрать 0.8B, 2B, 4B или 9B и скачать выбранную модель.
- Простые команды окон, мониторов и громкости выполняются кодом; модель используется для свободных формулировок и объяснений.
- Windows UI Automation читает элементы и вызывает их стандартные действия в отдельном процессе с ограничением времени.
- Снимок выбранного окна используется только по запросу, если интерфейс не дал достаточно информации. Обработка локальная, снимок в памяти. Перед подтверждённым нажатием по координатам проверяется, что изображение не изменилось.
- Контекст окна внутри разговора: «открой калькулятор», затем «сверни его». Последовательности до восьми шагов, проверка каждого результата, остановка при ошибке.
- Подтверждение ввода текста, клавиш и потенциально необратимых нажатий. Одноразовое подтверждение связано с конкретным окном и элементом, истекает через две минуты. Изменение интерфейса требует нового запроса.
- «Пятница, стоп» распознаётся отдельным режимом уже существующего Vosk-слушателя во время обработки команды, в том числе при свёрнутом окне. Обычные команды и Whisper сохранены.
- Прогресс и карточка подтверждения рядом с чатом; компактные настройки модели и разрешения снимков.

# 1.5 — Более спокойный интерфейс и интерактивное ядро

- Главный экран освобождён от повторяющихся статусов, рекламных подписей и технической колонки. Озвучка и свободный разговор доступны рядом с полем сообщения.
- Настройки разделены на голос и микрофон, оформление и систему. Подробные пояснения свёрнуты, основные действия видны сразу.
- Ядро реагирует на курсор, вращается перетаскиванием с инерцией и управляется стрелками. Пробел или кнопка рядом с ядром ставят анимацию на паузу.
- В оформлении можно выбрать реакцию на курсор, спокойное вращение или отсутствие движения. Режим сохраняется; учитывается системное уменьшение движения.
- Добавлены мягкие появления сообщений и реакции кнопок; анимация не работает на невидимой вкладке.

# 1.4.1 — Исправлена озвучка выразительными голосами

- Qwen3-TTS переведён на ускоренный движок с CUDA Graphs; сохранены Серена, Сохи и Анна и полностью локальная обработка.
- Образцы тембров подготовлены заранее: «Послушать голос» воспроизводит готовую запись без загрузки модели.
- Остановка, смена тембра и отмена запроса прекращают синтез в рабочем процессе. Отменённые запросы больше не занимают очередь.
- Добавлены ограничение ожидания, счётчик подготовки, защита от повторных нажатий и явная подсказка при выключенной озвучке ответов.
- Ответы воспроизводятся по предложениям; повторные короткие фразы берутся из ограниченного кеша в памяти.

# 1.4 — Голосовая активация «Пятница»

- Фоновый микрофон и локальная модель Vosk Small RU: обращение «Пятница» активирует распознавание команды, в том числе в свёрнутом окне.
- Можно сказать «Пятница, который час?» одной фразой или назвать имя, а затем команду с паузой до 8 секунд. Короткий сигнал подтверждает обращение.
- Новая настройка, постоянный индикатор и состояние в заголовке окна. Выбранный микрофон используется и для фонового режима.
- Во время ответа, ручной записи и проверки микрофона фоновый поток приостанавливается. «Отмена» после обращения отменяет команду; Esc выключает режим до повторного включения.
- Окружающая речь не попадает в историю и не записывается на диск. Аудиобуфер ограничен 20 секундами в памяти. В чат передаётся команда после обращения.
- Прослушивание завершается при полном выходе из приложения.

# 1.3 — Каталог команд и русские формулировки

- 90 действий и 1453 примера фраз: программы, папки, сайты, параметры Windows, звук, музыка, экран, поиск файлов, заметки, информация и вычисления.
- Общий справочник `shared/commands.json` используется интерфейсом и обработчиком, поэтому опубликованные варианты не расходятся с распознаванием.
- Категории, поиск по всем вариантам и склонениям, раскрывающиеся формулировки, копирование и подстановка в чат. Команды со своим текстом сначала подставляются для редактирования.
- Поддержаны вежливые обращения, числа словами и в косвенных падежах; названия файлов, папок, ссылки и заметки сохраняют исходное написание.
- Добавлены арифметика без исполнения кода, день недели, чтение последних заметок и 26 страниц параметров Windows.
- Все варианты проверены вместе с отрицаниями и вопросами об инструкциях. Полный список находится в `COMMANDS.md`.

# 1.2 — Выбор оформления

- Сине-голубая тема «Неон» стала основной: тёмный фон, нейтральный текст и мягкое свечение.
- В начале настроек добавлены четыре стиля с предпросмотром: Неон, Лаванда, Графит и Мята.
- Выбор мгновенно меняет все вкладки, кнопки, индикаторы и анимированное голосовое ядро; сохраняется после перезапуска.

# 1.1 — Микрофон, голос и управление Windows

- Отдельный блок выбора микрофона в начале настроек: названия устройств, обновление списка, проверка уровня, сохранение выбора. Проверка не отправляет речь в чат и автоматически заканчивается через 20 секунд.
- Исправлен запуск проводника: `explorer.exe` находится в папке Windows, а не в System32.
- Добавлен поиск установленных приложений через App Paths и меню «Пуск», а также сообщения о ненайденных программах.
- Команды понимают обращения «Пятница», «пожалуйста», «можешь открыть мне…».
- Добавлен каталог из 32 кнопок команд с поиском. Дополнительно доступны команды с параметрами: найти файл, найти в интернете, создать папку, открыть установленную программу или сайт.
- Добавлена локальная модель Qwen3-TTS 0.6B с тремя женскими голосами. Голоса Silero остались в группе «Быстрые».
- Ответы выразительным голосом воспроизводятся по фразам; следующая фраза готовится во время воспроизведения текущей.
- Исправлены описания возможностей помощницы: модель не должна утверждать, что выполнила действие, если его не выполнил обработчик.

## Примеры команд

«Пятница, можешь открыть мне проводник, пожалуйста?»

«Открой браузер», «Открой Telegram», «Открой Google Chrome», «Открой настройки Windows».

«Открой загрузки», «Открой документы», «Открой этот компьютер».

«Сделай громче», «Сделай тише», «Установи громкость на пятьдесят процентов», «Выключи звук», «Включи звук».

«Поставь музыку на паузу», «Продолжи музыку», «Следующий трек», «Предыдущий трек». Плеер должен поддерживать мультимедийные команды Windows.

«Покажи рабочий стол», «Восстанови окна», «Сделай скриншот» (сохраняется в `data/screenshots`).

«Найди файл отчёт» — поиск по имени в стандартных пользовательских папках с ограничением времени и десятью результатами. «Найди в интернете погоду» — открывает поиск в браузере; модель не читает веб-страницы.

«Создай папку на рабочем столе Проекты», «Сколько места на диске?», «Заряд батареи».

## Голоса

Qwen3-TTS потребляет дополнительную видеопамять и работает медленнее Silero. Первый запрос загружает модель. При нехватке памяти приложение предлагает закрыть другие GPU-программы или выбрать быстрый голос. Все веса и среда установлены локально, ключи API не используются.

Новый голосовой процесс отделён от основного Python-окружения, чтобы CUDA-библиотеки не мешали распознаванию речи. Он завершается вместе с приложением.
